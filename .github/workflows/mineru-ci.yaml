name: CI — Mineru

concurrency:
  group: ci-mineru-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

on:
  pull_request:
    paths:
      - "mineru/**"
  push:
    branches-ignore: [main]
    paths:
      - "mineru/**"
      - ".github/workflows/**"

jobs:
  build:
    uses: ./.github/workflows/build.yaml
    with:
      container: "mineru"
      push: false
    secrets: inherit
