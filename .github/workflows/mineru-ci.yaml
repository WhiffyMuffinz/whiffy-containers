name: CI — Mineru

on:
  pull_request:
    paths:
      - "mineru/**"
  push:
    branches-ignore: [main]
    paths:
      - "mineru/**"

jobs:
  build:
    uses: ./.github/workflows/build.yaml
    with:
      container: "mineru"
      push: false
    secrets: inherit
