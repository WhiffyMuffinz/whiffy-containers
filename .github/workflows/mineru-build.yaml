name: Build Fileflows Container

on:
  push:
    branches: [main]
    paths:
      - "mineru/**"
  workflow_dispatch:

jobs:
  call-build-workflow:
    uses: ./.github/workflows/build.yaml
    with:
      container: "mineru"
      push_on_main: true
    secrets: inherit
